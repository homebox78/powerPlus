# config 폴더 전체 비추적 — 비밀 설정(DB·메일·FTP)·SSH 키 포함, 절대 커밋 금지
# 단 값이 전부 플레이스홀더인 템플릿(config.example.php)만 추적해 신규 환경 셋업이 가능하게 한다.
/config/*
!/config/config.example.php
vendor/
*.log
.env

# 업로드된 자산 이미지(사용자 데이터)는 추적 안 함 — .htaccess 만 유지
/uploads/*
!/uploads/.htaccess

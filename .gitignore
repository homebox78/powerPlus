node_modules/
dist/
.env
*.log
.DS_Store

# 비밀키 / 배포 정보 — 절대 커밋 금지
*.pem
*.key
deploy.env

# 원본 자산 대용량 폴더 — git 아닌 서버(uploads)에 보관
/data/

# 설치 exe는 git에 포함(다른 PC에서 pull하면 바로 받도록) — 배포/사용자/powerPlus_Setup.exe 추적

# data 아카이브(FTP로 직접 이동, git 제외)
data.7z
data*.7z
data*.zip

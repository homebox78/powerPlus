// API/정적자원 베이스 URL.
// 운영 서버(hom2box) 절대경로 — 애드인이 어디서 호스팅되든(로컬 dev/서버/PowerPoint) 동일하게 동작.
// (서버 /api 는 CORS 허용 + OPTIONS 처리되어 교차출처에서도 OK)
export const API_BASE = "https://hom2box.com/powerPlus/api";
export const PUBLIC_BASE = "https://hom2box.com/powerPlus";
